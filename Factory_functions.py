# follow some TDD

def make_dough(ingedient1, ingedient2):
    # when given water in ingredient1 and flour in ingredient 2
        # return dough
    if ingedient1 == 'water' and ingedient2 == 'flour' or ingedient1 == 'flour' or ingedient2 == 'water':

        return "dough"
    else:
        return 'not dough'